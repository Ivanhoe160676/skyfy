import 'package:flutter/material.dart';

class EmptyLegsScreen extends StatelessWidget {
  const EmptyLegsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
