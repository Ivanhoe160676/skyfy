import 'package:flutter/material.dart';

class AddAircraftScreen extends StatelessWidget {
  const AddAircraftScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hola Mundo'),
      ),
    );
  }
}
