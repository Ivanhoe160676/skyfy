import 'package:flutter/material.dart';

class HomePilotScreen extends StatelessWidget {
  const HomePilotScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hola Mundo'),
      ),
    );
  }
}
